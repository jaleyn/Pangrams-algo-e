# Pangrams-algo-e
The function collects all letters from the input string, ignoring case, into a set. If the set contains all 26 letters of the alphabet, it returns "pangram"; otherwise, it returns "not pangram".
